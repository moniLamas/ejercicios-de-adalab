'use strict';

function parOdd (number) {
    if (`${number} % 2 === 1`) {
        console.log(`El número ${number} es par`);
    } else {
        console.log(`El número ${number} es impar`)
    };

    return number;
}

console.log(9);


